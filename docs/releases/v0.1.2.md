2026-09-08 重新构建全部平台安装包：修复 Android APK 打开即闪退的问题，移除已失效的旧授权码存储初始化调用，并增加 APK/AAB 成品原生方法检查。版本号保持 0.1.2，Android 和 Windows 继续使用原项目固定签名。

本次更新重点修复实时收信、已读状态和睦头模式页面切换，并为桌面与 Android 建立低开销后台收信能力。

- 修复从无顶部图片页面进入带顶部图片页面时邮件内容闪烁的问题，减少不必要的页面与阅读器重建。
- 修复首次切换已读/未读偶发失败、图标与状态不一致、侧栏未读计数回跳的问题。
- 自动收信优先同步收件箱，新邮件写入缓存后立即刷新列表、未读计数和系统通知。
- 改进 IMAP IDLE 保活、断线重连与兜底检查，减少服务端已收到邮件而客户端长时间无更新的情况。
- macOS、Windows 和 Linux 增加托盘常驻与可选登录启动；关闭窗口后可继续收信并发送系统通知。
- Android 增加独立于界面的前台收信服务、网络恢复、开机任务和 JobScheduler 兜底检查。
- 界面和后台入口共享缓存、同步状态及账户锁，避免重复拉取、旧任务覆盖新状态和取消竞争。
- 安静检查跳过正文下载、完整索引和无变化缓存写入；后台同步最多同时处理两个账户，Android 唤醒锁仅在短时传输期间持有。
- 增加 Android 后台服务构建检查，以及状态管理、同步取消、实时到达和响应式界面的回归测试。

下载时请选择对应平台的安装包。macOS 提供通用 DMG，Windows 提供 MSI 和 EXE，Android 提供 APK；AAB 用于分发。

Android 15 及以上会限制 `dataSync` 前台服务的后台运行时长；达到系统时限后由定期任务继续检查，重新打开应用后恢复实时连接。厂商省电策略、强行停止应用和断网仍可能延迟提醒。

macOS 包采用 ad-hoc 签名，未经过 Apple 公证；如首次打开被拦截，可在“系统设置 → 隐私与安全性”中允许。Windows 使用项目自签名证书，Android 使用项目固定签名。下载校验值见 SHA256SUMS.txt。
